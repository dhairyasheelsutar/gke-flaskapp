apiVersion: apps/v1
kind: Deployment
metadata:
  name: gke-flaskapp
spec:
  selector:
    matchLabels:
      app: gke-flaskapp
  replicas: 3
  template:
    metadata:
      labels:
        app: gke-flaskapp
    spec:
      containers:
      - name: flaskapp
        image: dhiraj6898/gke-flaskapp:v1
        resources:
          limits:
            memory: "128Mi"
            cpu: "250m"
        ports:
        - containerPort: 5000
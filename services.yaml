apiVersion: v1
kind: Service
metadata:
  name: gke-pubsub-flaskapp
spec:
  type: LoadBalancer
  selector:
    app: gke-pubsub-flaskapp
  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP